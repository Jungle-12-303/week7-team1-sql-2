id: TASK-T08
goal: WHERE id 범위 조회(`>`, `>=`, `<`, `<=`)를 파서/실행 경로에 반영한다.
depends_on:
  - TASK-T04
  - PLAN-03
inputs:
  - docs/plan/03-implementation-steps.md
outputs:
  - WHERE 비교 연산자 확장 파서
  - id 범위 조회 시 B+ 트리 리프 순회 경로
  - 범위 조회 단위 테스트
constraints:
  - 기존 `WHERE id = ?` 경로는 유지한다.
  - id 이외 조건은 기존 선형 스캔 경로를 유지한다.
done_when:
  - "WHERE id > ?, >= ?, < ?, <= ? 쿼리가 파싱된다."
  - "id 범위 조건이 B+ 트리 인덱스 경로로 실행된다."
  - "범위 조회 테스트가 통과한다."
owner: agent:auto-implementer
status: done

# T08
- 실행: WHERE 비교 연산자 확장 + id 범위 조회 인덱스 경로 반영
- 결과: id equality + id range 모두 인덱스 기반 조회 가능

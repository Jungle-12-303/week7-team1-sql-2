id: TASK-T04
goal: WHERE id 조건을 인덱스 경로로 분기한다.
depends_on:
  - TASK-T03
  - PLAN-02
inputs:
  - docs/plan/02-tech-design.md
outputs:
  - id 전용 조회 fast path
constraints:
  - 일반 조건 조회는 기존 선형 탐색을 유지한다.
done_when:
  - "WHERE id = ? 는 index_find를 사용한다."
  - "WHERE other = ? 는 선형 탐색을 사용한다."
owner: agent:auto-implementer
status: todo

# T04
- 실행: SELECT 분기 적용
- 결과: 인덱스/선형 경로 분리
